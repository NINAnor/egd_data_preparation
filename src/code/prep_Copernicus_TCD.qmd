---
title: "Prepare Copernicus High Resolution Layer Tree Cover Density data"
folderName: code # Leave to be filled out by ecRxiv during typesetting (name of the folder under 'indicators/'). 
# If it's a single indicator being reported, the folder name should be the same as the indicatorID. 
# If there are multiple ID's then the folder name could look something like XX_YYY_ZZZ-ÅÅÅ.
country: Norway
continent: Europe
yearAdded: 2026 # the year when the indicator was first submitted to ecRxiv
yearLastUpdate: 2026
status: complete # options are complete, incomplete, or deprecated
Version: "0.1.0" # See wiki page: https://github.com/NINAnor/ecRxiv/wiki#versioning
VersionComment: 'First version. The code has been adapted from https://github.com/NINAnor/ecRxiv/tree/main/indicators/NO_TCCD_001_104. Template adapated from Anders Kolstad ecRxiv repository: https://github.com/NINAnor/ecRxiv.'
url: https://github.com/NINAnor/egd_data_preparation
# To be filled out by ecRxiv during the typesetting, when they will replace XX_YYYY_ZZ with the name of the root folder for the current indicator(s). It should be the same as the indicatorID, but if there are more indicator IDs presented in the same folder, the folder name might be different, e.g. XX_YYYY_AAA-BBB 
format: 
  html:
    embed-resources: true
    code-fold: true
    toc: true
    toc-title: Contents
    toc-depth: 3
    smooth-scroll: true
execute: 
  cache: true
author:
  - name: Sylvie Clappe             # Enter name
    email: sylvie.clappe@nina.no  # Enter email
    affiliations:
      - id: myID
        name: Norwegian Institute for Nature Research # Enter affiliations
  - name: Bálint Czúcz              
    email: balint.czucz@nina.no             #  Enter subsequent authors like this, or remove if not relevant
    affiliations:
      - ref: myID               # To reuse affiliations reference the id like this
AuthorList: Clappe, S., Czúcz, B.
date: August 11, 2026
callout-icon: false
lightbox: true
css: ../../../style.css
code-links:
      - text: Add a review
        icon: github
        href: https://github.com/NINAnor/ecRxiv
hide: TRUE # Change to FALSE
---

<!--# This is a template for how to document the indicator analyses. Make sure also to not change the order, or modify, the headers, unless you really need to. This is because it easier to read if all the indicators are presented using the same layout. If there is one header where you don't have anything to write, just leave the header as is, and don't write anything below it. If you are providing code, be careful to annotate and comment on every step in the analysis. Before starting it is recommended to fill in as much as you can in the metadata file. This file will populate the initial table in your output.-->

<!--# Load all you dependencies here -->

```{r setup}
#| include: false
#| cache: false
library(knitr)
library(tidyverse)
library(kableExtra)
library(here)
library(yaml)
library(tibble)
library(conflicted)
library(tidyverse)
library(dplyr)
library(terra)
library(carrier)
library(mirai)

# weird workarounds for cache issues
conflicts_prefer(dplyr::filter)
conflicts_prefer(dplyr::lag)
conflicts_prefer(dplyr::select)
pull<-dplyr::pull

knitr::opts_chunk$set(echo = TRUE)
```

<!--# The following parts are autogenerated. Do not edit. -->


```{r source}
#| echo: false
#| cache: false

source(here::here("_common.R"))
```


```{r reviewers}
#| echo: false
#| results: asis

if (!is.null(yaml_data$codeReviewers)) {
  reviewers <- purrr::map_chr(yaml_data$codeReviewers, "name")
  
  htmltools::HTML(paste0(
    '<div id="title-block-header" class="quarto-title-block default">
       <div class="quarto-title-meta">
         <div>
           <div class="quarto-title-meta-heading">Reviewed by</div>
           <div class="quarto-title-meta-contents">',
    paste(reviewers, collapse = "; "),
    '</div>
         </div>
       </div>
     </div>',
    sep = ""
  ))
}
```

> **Recommended citation**: `r paste(auth, " ", year, ". ", "v. ", version, ": ", url, sep="")`



::: {.callout-tip collapse="true"}
## Metadata
```{r tbl-meta2}
#| tbl-cap: 'Indicator metadata'
#| echo: false
#| warning: false
meta |>
  dplyr::filter(Variable %in% c(
    "Indicator ID",
    "Indicator Name",
    "verbatimName",
    "Continent",
    "Country",
    "Year added", 
    "Last update",
    "Version",
    "Version comment")) |>   
  kable()

```
:::

::: {.callout-tip collapse="true"}

## Log

<!--# Update this logg with short messages for each update -->
- 11 August 2026 - First draft version
:::


<hr />

<!--# Document you work below.  -->

## 1. Summary
This code shows how to download and pre-process Copernicus High Resolution Layer (HRL) Tree Cover Density (TCD) data to compile the TCCD indicators from ecRxiv: https://github.com/NINAnor/ecRxiv/tree/main/indicators/NO_TCCD_001_104.


## 2. Data characteristics
These data are produced by Copernicus: https://land.copernicus.eu/en/products/high-resolution-layer-forests-and-tree-cover.

### 2.1 Spatial and temporal resolution and extent
We downloaded the data for the whole on Norway (excluding Svalbård), for the years 2018, 2019, 2020, 2021. Spatial resolution is 10m x 10m.

### 2.2 Sampling design
The HRL TCD data are generated using state-of-the-art automated image processing 
methods that are applied to systematically acquired, high-resolution satellite image time series 
from the Copernicus space segment (i.e. Sentinel-2). Layers have been harmonised across time, allowing for time series comparisons. See [Algorithm Teoretical Basis Document](https://library.land.copernicus.eu/products/High_Resolution_Layer_Vegetated_Land_Cover_Characteristics_ATBD_v2.html) and [Product user manual](https://land.copernicus.eu/en/technical-library/product-user-manual-tree-cover-and-forests-2018-present/@@download/file).


### 2.3 Original units
The original unit of Copernicus HRL TCD is percentages (the share of tree cover areas in the pixel).


### 2.4 Instructions for citing, using and accessing data
Openly available[here](https://land.copernicus.eu/en/products/high-resolution-layer-forests-and-tree-cover/tree-cover-density-2018-present-raster-10-m-europe-yearly#download)


### 2.5 Additional comments about the dataset
Other metadata can be found in the [Product user manual](https://land.copernicus.eu/en/technical-library/product-user-manual-tree-cover-and-forests-2018-present/@@download/file)


## 3. Uncertainties
### 3.1 General uncertainties
Uncertainties have several sources all taking roots in the modelling used to process the Sentinel-2 raw data. They can come from the pre-processing of the raw data necessary to correct the images (e.g., radiometrical/sensor calibration; geophysical corrections; cloud cover corrections;creation of the mosaic). But they also arise when the pre-processed data are classified through machine learning to produce the final layer of imperviousness. Classification is never definitive and produces probabilities for a pixel to belong to a specific class. 

### 3.2 Reported uncertainties
Copernicus reported "Confidence layers" for each of the TCD datasets. In their technical guide, they explain that, they report the "Total Uncertainty" which is defined as the sum of data and knowledge uncertainty. "data uncertainty" is a measure for the data complexity due to noise or overlapping classes. While "knowledge uncertainty" is obtained through the CatBoost algorithm by using virtual ensembles. Basically, CatBoost runs on several data ensembles and compiles the mean value. THe "Knowledge uncertainty" is then the variance of the al the means comiled.

The confidence values are pixel-based and range from 0 to 100. In practice only values 50-100 are used as values below 50% would indicate a higher confidence in the opposite class. The recommended interpretation state that pixels with values ranging 75-100 are of high confidence, 50-75 of moderate confidence, and below 50 of low confidence.

The confidence layers are available for all years.


## 4. Data download

### 4.1 Copernicus TCD
We download the data through API, using [WEkEO](https://help.wekeo.eu/en/articles/6416936-how-to-download-data-through-wekeo). 

For transparency, we show below a code to read the raster data from the zip files that are given by Copernicus. 
*!Attention: this code, writes the rasters on the disk of the users.*
```{r}
#| eval: false
#| code-summary: "Read the .tif from a zip file"

# Call functions
source(here::here("src", "functions", "list_zip_files.R"))
source(here::here("src", "functions", "extract_zip_files.R"))

# Path to the downloaded Copernicus data
path_tcd <- "/data/P-Prosjekter2/412413_2023_no_egd/git_data/TCD_Copernicus"

# List datasets' paths to read and their names
files_tcd_read <- files_list_sev_fn(path_tcd)

# List datasets path (here we remove the zip marker and the name of the file)
files_tcd_write <- str_remove(files_tcd_read[[1]], "CLMS.*") %>%
                      str_remove("/vsizip/\\{")

# Run the function to read and write the rasters from the zip files directly
library(mirai)
daemons(10)
map(seq_along(files_tcd_read[[1]]),
    in_parallel( ~process_raster_fn(path_read = files_tcd_read[[1]][.x],
                                    path_write = files_tcd_write[.x],
                                    files_names = files_tcd_read[[2]][.x]),
                 process_raster_fn = process_raster_fn,
                 files_tcd_read = files_tcd_read,
                 files_tcd_write = files_tcd_write
    )
)

daemons(0)

```

### 4.2 Copernicus IMD Confidence layers
Now we do the same for the confidence layers.
```{r}
#| eval: false
#| code-summary: "Read the .tif from a zip file"

# Call functions
source(here::here("src", "functions", "list_zip_files.R"))
source(here::here("src", "functions", "extract_zip_files.R"))

# Path to the downloaded Copernicus data
path_tcd_unc <- "/data/P-Prosjekter2/412413_2023_no_egd/git_data/TCD_Copernicus_uncertainties"

# List datasets' paths to read and their names
files_tcd_read <- files_list_sev_fn(path_tcd_unc)

# List datasets path (here we remove the zip marker and the name of the file)
files_tcd_write <- str_remove(files_tcd_read[[1]], "CLMS.*") %>%
                      str_remove("/vsizip/\\{")

# Run the function to read and write the rasters from the zip files directly
library(mirai)
daemons(10)
map(seq_along(files_tcd_read[[1]]),
    in_parallel( ~process_raster_fn(path_read = files_tcd_read[[1]][.x],
                                    path_write = files_tcd_write[.x],
                                    files_names = files_tcd_read[[2]][.x]),
                 process_raster_fn = process_raster_fn,
                 files_tcd_read = files_tcd_read,
                 files_tcd_write = files_tcd_write
    )
)

daemons(0)

```



## 5. Data cleaning and formatting
Once the rasters (.tf) have been extracted and written on the disk, they can be read to create a virtual raster that will be used for the subsequent analyses later on.

### 5.1 Copernicus IMD

```{r}
#| eval: false
#| code-summary: "Create Copernicus IMD virtual raster"

# Code co-created by Sylvie Clappe and Balint Czucz

# Create or load the IMD virtual rasters
terra::setGDALconfig("GTIFF_SRS_SOURCE", "EPSG") # to quiet a GDAL warning
path_ycd_list <- dir(path_tcd, recursive = FALSE, full.names = TRUE)
yrs_tcd <- str_sub(path_imd_list, start = -4)
file_tcd <- map_vec(seq_along(path_tcd_list), function(i){paste0(path_tcd_list[i], "/TCD_", yrs_tcd[i], ".vrt")})

map(seq_along(file_tcd), ~ 
               
        # if it does not exist yet, create the virtual raster
       if (!file.exists(file_tcd[.x])) {
            path_tcd_list[.x] %>%
            list.files(pattern= '.tif$', all.files= TRUE, full.names= TRUE) %>% # list of tiles
            vrt(filename= file_tcd[.x]) 
         
        # otherwise just load it 
       } else {
        rast(file_tcd[.x])  
       }
    )

```


### 5.2 Copernicus IMD Confidence layers
```{r}
#| eval: false
#| code-summary: "Create Copernicus IMD confidence virtual raster"

# Code co-created by Sylvie Clappe and Balint Czucz

# Create or load the IMD virtual rasters
terra::setGDALconfig("GTIFF_SRS_SOURCE", "EPSG") # to quiet a GDAL warning
path_tcd_list <- dir(path_tcd_unc, recursive = FALSE, full.names = TRUE)
yrs_tcd <- str_sub(path_tcd_list, start = -4)
file_tcd <- map_vec(seq_along(path_tcd_list), function(i){paste0(path_tcd_list[i], "/IMD_", yrs_tcd[i], ".vrt")})

map(seq_along(file_tcd), ~ 
               
    # if it does not exist yet, create the virtual raster
   if (!file.exists(file_tcd[.x])) {
        path_tcd_list[.x] %>%
        list.files(pattern= '.tif$', all.files= TRUE, full.names= TRUE) %>% # list of tiles
        vrt(filename= file_tcd[.x]) 
     
    # otherwise just load it 
   } else {
    rast(file_tcd[.x])  
   }
   
  )

```


## 6. Export file
Virtual raster already exported in the previous code.

## 7. Author contributions

<!-- Fill in CReDIT and MeRIT statments to clarify the contributions from individual authors. For MeRIT, see https://www.nature.com/articles/s41467-023-37039-1. For CRedit, see https://credit.niso.org/. You can start with the template structur below, and just delete the rows you dont use.  -->

**CRediT statement**

* Conceptualization: SC
* Methodology: SC & BC
* Data curation & formatting: SC
* Writing – original draft: SC
* Writing – review & editing: BC
* Document structure: SC & AK


**MeRIT attribution**

The structure of the document has been taken from ecRxiv's template for condition indicators, created by Anders Kolstad (AK). SC adapted it to the purpose of the project.

**Study design**
The study was designed by SC and BC.

**Data processing**
Raw data were downloaded by SC. They were curated and cleaned by SC and BC. Code was written by both SC and BC.


::: {.callout-tip collapse="true"}
## Session Info
<!-- You can leave this last part as it is, unless you have a good reason to alter it. -->

This information is mainly aimed at developers that need to recreate the R environment that produced the html.
Important note: this information is about the R session that was running when the html was rendered, and if some of the code chunks were not evaluated when the html was rendered, they may have been executed in a different environment.  

```{r sessionInfo}
#| echo: false
sessionInfo()
```

:::
